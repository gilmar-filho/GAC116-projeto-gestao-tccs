# Copie para .env e ajuste os valores

SECRET_KEY=troque-por-uma-chave-secreta
DEBUG=True
ALLOWED_HOSTS=*

# Em produção, use a connection string do Postgres (Neon):
# DATABASE_URL=postgres://usuario:senha@host/banco?sslmode=require
# Sem DATABASE_URL definida, o projeto usa SQLite local.
DATABASE_URL=

server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Roteamento do SPA: qualquer rota cai no index.html.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
